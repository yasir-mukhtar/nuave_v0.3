# Session Summary — March 9, 2026

## Support Page (`/support`)

**Contact form page** (`src/app/support/page.tsx`)
- Header with Nuave logo, smart back button in hero (uses `router.back()`, falls back to `/`)
- Hero: "Hubungi Kami" with badge "Kami siap membantu"
- Form fields: Nama, Email, Kategori (dropdown: Pertanyaan Umum, Kemitraan, Keluhan, Pembayaran, Bug, Saran Fitur, Privasi, Lainnya), Subjek, Pesan
- Submits via POST to `/api/support` — sends email directly via Resend
- Success state with "Pesan terkirim!" confirmation
- Right sidebar cards: "Butuh Respon Cepat?" (Instagram + X icons with links), "Email Langsung" (hello.nuave@gmail.com)

**API route** (`src/app/api/support/route.ts`)
- Sends formatted HTML email via Resend SDK
- To: hello.nuave@gmail.com, CC: mail.yasirmukhtar@gmail.com
- ReplyTo: sender's email address
- HTML escaping on all user input to prevent XSS
- Resend client instantiated per-request (not module-level) to avoid build errors without env var

**Environment variable added:** `RESEND_API_KEY` (in .env.local and Vercel)
**Domain verified:** nuave.ai on Resend, sending from `support@nuave.ai`

## Sidebar Updates (`src/components/layout/Sidebar.tsx`)

- Removed "Panduan" and "Pengaturan" from bottom links — only "Bantuan" remains, linking to `/support`
- **User account popover**: click user card → popover shows name, email, "Keluar" logout button
  - Calls `supabase.auth.signOut()` then redirects to `/`
  - Closes on outside click
  - Chevron rotates on open/close
- **User card styling**: white background, border, rounded corners (8px), subtle shadow with hover elevation
- Brand name removed from both user card and popover
- `userEmail` prop added — passed from dashboard layout via `supabase.auth.getUser()`

## Popover Animations (`src/app/globals.css`)

Two reusable CSS classes added:
- `.popover-up` — fade + blur(4px) + scale(0.97) + translateY(4px), 180ms, cubic-bezier(0.16, 1, 0.3, 1)
- `.popover-down` — same but translates from -4px (downward opening)

Applied to:
- Sidebar user popover (`.popover-up`)
- Topbar workspace dropdown (`.popover-down`)
- VisibilityChart date filter dropdown (`.popover-down`)

## Footer Update (`src/components/Footer.tsx`)

- Added "Bantuan" link pointing to `/support` in legal links row

## Prompt Page (`src/app/(dashboard)/dashboard/prompts/page.tsx`)

**Prompt intelligence hub** at `/dashboard/prompts`, accessible from sidebar "Prompt" link.

- **Summary cards**: Total Prompt count, Tingkat Sebutan (mention rate, color-coded by threshold), Awareness stage rate, Decision stage rate
- **Filter bar**: Segmented tabs (Semua/Disebut/Tidak disebut) with count badges, workspace dropdown (shows when multi-brand), search input
- **Table**: Mention status icon, prompt text (truncated), brand name, funnel stage badge (Awareness=blue, Consideration=yellow, Decision=green), audit date in Indonesian format
- **Click row** → opens shared `PromptDetailModal` with full AI response
- **Data flow**: Fetches all workspaces → complete audits → audit_results, cross-references prompts table for stage info
- Empty and loading states handled

## Project Context Files

Moved root docs (`CLAUDE.md`, `CODEBASE_OVERVIEW.md`, `nuave-prd-v1_2.md`) into `project context/` folder.

## Commits

- `bbe114e` — `feat: add support page, sidebar user popover with logout, and popover animations`
- `e4442f4` — `feat: build prompt intelligence page at /dashboard/prompts`
